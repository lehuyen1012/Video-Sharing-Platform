import { StyleSheet, Text, View } from "react-native";

const Bookmark = () => {
    return (
        <View>
            <Text>Bookmark</Text>
        </View>
    );
};

export default Bookmark;

const styles = StyleSheet.create({});
